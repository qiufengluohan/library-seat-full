<view class="empty">
  <view class="empty-icon">{{icon}}</view>
  <view class="empty-text">{{text}}</view>
  <view class="empty-sub" wx:if="{{sub}}">{{sub}}</view>
  <view class="empty-btn" wx:if="{{btnText}}" bindtap="onTap">{{btnText}}</view>
</view>
