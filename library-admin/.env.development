# 开发环境配置
#
# 这里只允许出现"前端自己要知道的地址"。
# OneNET 的 product-id / api-key、JWT 密钥、数据库口令一律属于 library-seat 后端，
# 绝不能放进 VITE_ 变量——VITE_ 开头的值会被打进前端 bundle，等于公开泄露。
# 依据《四端统一最终方案》§41：Web 想直接调用 OneNET，禁止。

# 走 Vite 代理，见 vite.config.js。这样开发期不会有跨域问题。
VITE_API_BASE_URL=/api

# WebSocket 地址留空 = 与页面同源，由 Vite 代理转发到 localhost:8080
# 如需直连后端可改为 ws://localhost:8080
# VITE_WS_BASE_URL=
