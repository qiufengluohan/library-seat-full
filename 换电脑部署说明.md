# 换电脑部署说明

> 打包时间：2026-09-24 15:00　源机器：Windows 10 (19045) x64
> 这份文档只讲**新电脑上要装什么、要配什么**。项目本身的设计说明在
> `library-seat/README.md`、`library-admin/README.md`、`jidian-library/README.md` 和
> 两份《规范/方案》txt 里。

---

## 〇、包里没有的东西（必须自己补）

**所有密钥都不在这个包里**，这是刻意的。包内 `application.yml` 的每一项凭证都是
`${环境变量:空默认值}` 的形式，没有任何明文密钥。

| 没打进包 | 原因 | 从哪拿 |
|---|---|---|
| `设备导出_1789981048.xlsx` | 里面有 SEAT_001 / READER_01 的设备密钥 | 你本机 `Qoder/1/` 目录下，或 OneNET 控制台重新导出 |
| `library-admin-history-backup.bundle` | git 历史备份，运行不需要 | 你本机 `Qoder/1/` 目录下 |
| `.idea/`（两个工程都有） | **IDEA 会把运行配置里的环境变量明文写进 `.idea/workspace.xml`**，含微信 AppSecret | 新电脑上按下面第三节重新填一遍 |
| `library-admin/node_modules/` | 255 MB，用 `npm install` 重建 | — |
| `library-admin/dist/`、`library-seat/target/classes` | 构建产物 | — |
| MySQL 数据库 `library_seat` 的数据 | 数据库不在文件里 | 见第四节，或从旧电脑 `mysqldump` |

⚠️ 只要 `.idea/workspace.xml` 曾经出现在你发给别人的压缩包里，就等于泄露了
`WECHAT_APP_SECRET`。这次已经排除，但旧的那份包建议确认一下。

包内**有**一个预编译产物 `library-seat/target/library-seat-1.0.0.jar`（33 MB，含全部依赖），
它不含任何密钥。有它意味着新电脑只要装 JDK 就能跑后端，不装 Maven 也行。
不需要就删掉，`mvn package` 会重新生成。

---

## 一、要装的软件

| 软件 | 版本要求 | 实测参照 | 干什么用 | 不装会怎样 |
|---|---|---|---|---|
| **JDK** | 17 及以上 | 27 | 跑后端 | 后端起不来 |
| **Maven** | 3.8+（可选） | 3.9.16 | 从源码重新构建后端 | 只能用包里那个预编译 jar |
| **MySQL** | 8.0（5.7 未验证） | 8.0.44 | 数据库 | 后端启动即失败 |
| **Node.js** | 18+（Vite 5 要求） | 24.21.0 | Web 管理端 | 管理端起不来 |
| **微信开发者工具** | 稳定版即可 | — | 小程序 | 小程序打不开 |
| **cpolar**（或 ngrok / frp） | — | — | 把 OneNET 的 HTTP 推送打到本地后端 | 设备上报进不来，座位永远显示离线 |
| IDEA / VS Code | — | — | 编辑 | — |

`pom.xml` 里 `java.version=17`，但 **Lombok 已锁 1.18.48**（Boot 默认的 1.18.32 在 JDK 23+
上会静默不生成 getter/setter，编译还 SUCCESS、运行才炸）。用新 JDK 不必改任何配置。

装完逐条验证（cmd 或 PowerShell）：

```powershell
java -version        # 期望 17 以上
mvn -v               # 期望 Apache Maven 3.8+
node -v   ; npm -v   # 期望 v18+
mysql --version      # 期望 8.0.x
Get-Service mysql    # 期望 Status = Running（服务名以实际为准）
```

**Maven 镜像（国内建议）**：装过的那台机器 `~/.m2/settings.xml` 配了阿里云镜像。
新电脑没配的话第一次 `mvn package` 会慢或超时，可把旧电脑的 `settings.xml` 拷过去。

---

## 二、解压后的目录

```
library-seat-full/
├── library-seat/            后端 Spring Boot，端口 8080
├── library-admin/           Web 管理端 Vue3 + Vite，端口 3000
├── jidian-library/
│   └── jidian-library/      ← 小程序项目要导入这一层（project.config.json 在这）
├── model-Wh08f3Q71p.json    OneNET 物模型导入文件
├── 智慧图书馆…规范_最终版.txt
├── 智慧图书馆…四端统一最终方案.txt
├── 素材/                    毕设方案文档与截图，与运行无关
└── 换电脑部署说明.md         本文件
```

---

## 三、环境变量（**最关键的一节**）

### 3.1 清单

| 变量 | 必须？ | 不设会怎样 | 从哪拿 |
|---|---|---|---|
| `MYSQL_PASSWORD` | 是（若密码不是 `root`） | 连不上库，启动报 Access denied | 新电脑自己装的 MySQL 密码 |
| `WECHAT_APP_SECRET` | 要真机登录就必须 | `POST /api/auth/wechat-login` 直接返回"微信登录未配置"，启动日志有 WARN | 微信公众平台 → 开发管理 → 接口密码 |
| `ONENET_PUSH_SECRET` | 强烈建议（后端挂在公网隧道上时必须） | `/api/onenet/**` 免鉴权，任何人都能伪造设备上报；每次放行打一条 WARN | **自己随机生成**，同时填进 OneNET 推送 URL |
| `JWT_SECRET` | 建议 | 用仓库里那个公开占位串 → 别人能伪造任意用户/管理员 token | 自己随机生成 |
| `ONENET_API_KEY` | 要下行才需要 | 管理端的「修改阈值 / 测试蜂鸣器 / 强制释放」全失败（上行推送不受影响） | OneNET 控制台 → 产品开发 → 密钥 |
| `WECHAT_APP_ID` | 一般不用 | 默认已填 `wx0384c7d1e1e2d16d` | 换小程序才要改 |
| `ONENET_PRODUCT_ID` | 不用 | 默认已填 `Wh08f3Q71p` | — |
| `MYSQL_HOST` / `MYSQL_PORT` / `MYSQL_USERNAME` | 不用 | 默认 `localhost` / `3306` / `root` | — |

生成随机密钥（PowerShell，一条一个值）：

```powershell
[guid]::NewGuid().ToString("N")        # 32 位十六进制，够 JWT / push_secret 用
```

### 3.2 怎么设 —— 三种方法，选一种，别混

**A. PowerShell 会话级（最快，只在这一个窗口有效）**

```powershell
$env:MYSQL_PASSWORD     = '你的库密码'
$env:JWT_SECRET         = [guid]::NewGuid().ToString("N")
$env:ONENET_PUSH_SECRET = [guid]::NewGuid().ToString("N")
$env:WECHAT_APP_SECRET  = '从公众平台复制'
$env:ONENET_API_KEY     = '从 OneNET 复制'
mvn spring-boot:run        # 或者 java -jar target\library-seat-1.0.0.jar
```

窗口关掉就没了，下次要在**同一个窗口**重新设再起。**这个窗口别关。**

**B. IDEA 运行配置（一直有效，推荐日常用）**

`Run → Edit Configurations → 选中 LibrarySeatApplication → Environment variables`，
多个用**分号**分隔：

```
MYSQL_PASSWORD=xxx;JWT_SECRET=xxx;WECHAT_APP_SECRET=xxx;ONENET_PUSH_SECRET=xxx;ONENET_API_KEY=xxx
```

三条铁律：
1. **不要加引号** —— 这个输入框不像 shell，`A="abc"` 得到的值是 `"abc"`（长度 5）。
2. 改完必须**红色方块停掉再 Run**。热部署不会重读环境变量。
3. **不要勾 Store as Project file / Share** —— 配置会落成 `.idea/runConfigurations/*.xml`，
   密钥就跟着项目目录外流了。默认的 workspace 本地存储也可能被 `.idea` 整目录拷走，
   所以**永远不要把 `.idea` 发给别人或提交进仓库**。

**C. Windows 用户级（一次设好，全局有效）**

```powershell
setx MYSQL_PASSWORD "xxx"
```

`setx` **对已经开着的窗口和已经开着的 IDEA 都无效**，要注销重登（或重启 IDEA）才生效。
设完在**新窗口**里 `echo %MYSQL_PASSWORD%` 确认。

### 3.3 设没设对，日志会直接告诉你

启动后 grep 这几条 WARN（`JwtInterceptor` / `UserServiceImpl`）：

| 日志 | 含义 |
|---|---|
| `微信登录未配置（wechat.app-secret 为空）…` | `WECHAT_APP_SECRET` 没进这个 JVM |
| `未配置 onenet.push-secret，/api/onenet/… 处于免鉴权状态` | `ONENET_PUSH_SECRET` 没进这个 JVM |
| 两条都没有 | 环境变量已生效 |

---

## 四、数据库（新电脑要重建）

```powershell
cd library-seat\src\main\resources\db
mysql -uroot -p < 01_schema.sql     # 建库 library_seat + 9 张表
mysql -uroot -p < 02_seed.sql        # 种子数据，幂等，可反复执行
```

两个脚本都用 `INSERT IGNORE`，重复执行不会覆盖你改过的密码和座位信息。

种子数据只有两条：**管理员 `admin` / `Admin@123`**（BCrypt 哈希）和
**一个座位 `A4-203`（绑定设备 `SEAT_001`）**。上线/答辩前必须改管理员密码。

学生记录刻意不种 —— 由微信登录自动创建。`device_id` 上有 UNIQUE 约束，
所以不能插入没有真实设备的座位来充数（那种座位永远不上报，管理端一直显示离线）。
以后在 OneNET 上加了 SEAT_002，就照 `02_seed.sql:54` 那行注释的格式加座位。

想把旧电脑的业务数据带过去：

```powershell
# 旧电脑
mysqldump -uroot -p --default-character-set=utf8mb4 library_seat > seat_dump.sql
# 新电脑：先跑 01_schema.sql 建库，再
mysql -uroot -p library_seat < seat_dump.sql
```

---

## 五、启动三端

| 端 | 命令 / 操作 | 地址 |
|---|---|---|
| 后端 | `cd library-seat` → `mvn spring-boot:run`（或 `java -jar target\library-seat-1.0.0.jar`） | http://localhost:8080 |
| Web 管理端 | `cd library-admin` → `npm install` → `npm run dev` | http://localhost:3000，会自动开浏览器 |
| 小程序 | 微信开发者工具 → 导入项目 → 选 `jidian-library\jidian-library` 这一层 | 模拟器 |

管理端登录 `admin` / `Admin@123`。Vite 已把 `/api` 和 `/ws` 代理到 8080，不用配跨域；
要连别的后端就改 `library-admin/vite.config.js` 里的 `BACKEND` 常量。

**npm 报"禁止运行脚本"**（新电脑常遇到）：

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

然后**开一个新窗口**再 `npm run dev`（策略在进程启动时读取）。或者把 VS Code / IDEA
的默认终端换成 Command Prompt 或 Git Bash。

---

## 六、内网穿透 + OneNET 数据推送（换电脑必然要重配）

产品、设备、物模型都在 OneNET 云上，**跟电脑无关，不用重建**。要重配的只有推送 URL，
因为新电脑的隧道域名是新的。

```
后端(8080) ──▶ cpolar 隧道 ──▶ https://<随机域名>.cpolar.top ──▶ OneNET 数据推送
```

1. 起隧道，指向本地 8080：
   ```powershell
   cpolar http 8080
   ```
   本地管理面板 http://localhost:9200（`curl -s http://localhost:9200/api/tunnels` 也能看域名）。
   免费版**每次重启域名会变**，OneNET 里那条 URL 就得跟着改一次。
2. OneNET 控制台 → 产品开发 → **数据推送 → HTTP 全局推送**：
   - url：`https=<新隧道域名>/api/onenet/device-data?push_secret=<第三节那串>`
   - 消息加密方式：**明文**（选"安全"的话报文是 AES 密文，后端解不了）
   - **生效状态开关必须打开**（这个开关默认关，最容易被忽略）
   - 点保存会触发一次 URL 校验
3. 校验能过的原因写在代码里：`OneNetController` 有个 `@GetMapping("/device-data")`，
   把 OneNET 传来的 `msg` 查询参数**原样回显**、`Content-Type: text/plain`。
   返回 `success` 或 `{"code":0}` 都算校验失败（多了引号/符号）。
4. **一个已知的坑，代码里已经处理了**：OneNET 往你的 URL 追加自己的参数时用的是 `?`
   而不是 `&`，于是 `push_secret` 的值会变成 `<密钥>?nonce=xxxxxxxx`。
   `JwtInterceptor.checkPushSecret` 会截掉第一个 `?` 之后的部分再比对。
   新电脑上如果密钥校验报 `是否携带密钥=true`，先看那条 WARN 的 `实收长度` 和
   `实收是否带?`，不要凭猜。

下行（管理端点「修改阈值/测试蜂鸣器/强制释放」）要 `ONENET_API_KEY` 才通。
Studio 的 API 路径可能随版本调整，都留在 `application.yml` 的
`onenet.property-set-path` / `service-invoke-path`，**返回 404 就改这两行，不用改 Java**。

---

## 七、微信侧的两件事

1. **IP 白名单**：微信公众平台 → 开发管理 → 开发设置 → IP 白名单。
   **换电脑、换网络（尤其手机热点）都会变公网出口 IP**，不在白名单时
   `code2Session` 报 `errcode=40164`，小程序端表现为登录 401。
   查自己当前的公网 IP（注意要用 IPv4）：
   ```powershell
   curl -4 https://ifconfig.me
   ```
   家里/实验室通常是动态 IP，每次变了都要来加。
2. **合法域名**：正式预览/发布要在公众平台把后端地址同时加到
   **request 合法域名**和**socket 合法域名**两个入口（两个独立的地方，只加前一个
   WebSocket 连不上）。开发期 `project.config.json` 里 `urlCheck: false` 已经关了校验，
   模拟器上不用管；**真机预览时 `baseUrl` 要填局域网 IP**（如 `http://192.168.1.10:8080`），
   且手机和电脑连同一个 Wi-Fi。

小程序后端地址只在 `jidian-library/jidian-library/miniprogram/utils/config.js`
的 `server.baseUrl` 一处，WebSocket 地址由它推导（`https→wss`、`http→ws`）。

---

## 八、从零开始的验证顺序

按这个顺序做，每一步都能单独判断成败：

| # | 操作 | 期望 |
|---|---|---|
| 1 | 只起后端 | 日志无 `未配置 …` 那两条 WARN，无数据源报错 |
| 2 | `curl -i http://localhost:8080/api/seats` | `401`（JWT 拦截正常，说明服务活着） |
| 3 | `curl -i -X POST "http://localhost:8080/api/onenet/device-data?push_secret=<密钥>?nonce=abcdefgh" -H "Content-Type: application/json" -d "{}"` | `200`。注意故意写成 `?nonce=`，同时验证截断逻辑 |
| 4 | 起 cpolar，用隧道域名重复第 3 步 | 同样 `200` |
| 5 | OneNET 数据推送填 URL → 保存 | 校验通过（不是"返回结果未正确返回msg值"） |
| 6 | 设备下一次上报 | 后端日志 `设备在线状态变化: deviceId=SEAT_001, online=true` |
| 7 | 管理端座位列表 | 离线徽标消失；`seat_shadow` 表开始有行 |
| 8 | 小程序点「微信登录」 | 拿到 JWT，进首页 |

第 6 步如果打的是 `设备上报缺少 device_id，已忽略`：说明 OneNET 报文结构和
`OneNetServiceImpl.normalize` 预期的又不一样了。打开
`application.yml:128` 那行 debug（**包里已经是打开状态**）就能看到
`OneNET 上行原始报文: {...}`，照着报文改 `normalize` 一个方法即可，业务代码不用动。

---

## 九、当前代码状态里两处"记得改回去"

1. `library-seat/src/main/resources/application.yml:128` 的
   `com.example.libraryseat.service.impl: debug` **现在是打开的**（联调期间方便看原始报文）。
   它会整段打印 OneNET 推送内容，答辩录屏/上线前建议注释掉。
2. 种子 `admin` 密码是 `Admin@123`，`jwt.secret` 的默认值是个公开占位串。
   公网部署前：改管理员密码 + 用环境变量注入随机 `JWT_SECRET`。

---

## 十、这个包是怎么来的

从 `C:\Users\20622\Desktop\AI\Qoder\1\` 复制，排除了 `node_modules/`、`dist/`、
`target/classes`、`.idea/`、`.vscode/`、`*.iml`、`设备导出_*.xlsx`、
`library-admin-history-backup.bundle`。打包前跑过：

```
mvn -DskipTests package     # 退出码 0，产物 target/library-seat-1.0.0.jar (33 MB)
```

并对整棵目录做过密钥扫描（长 base64/hex 串 + `app-secret|api-key|password` 赋值式命中），
结果只有文档链接、Java 类名和 `your_api_key_here` 这类占位符，无真实凭证。
本包不含 `.git` 目录（源目录本来就不是 git 仓库）。
