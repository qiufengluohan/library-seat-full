# 生产环境配置
#
# 同样只放地址，不放任何密钥。部署时用 Nginx 把 /api 与 /ws 反向代理到
# library-seat（Spring Boot，默认 8080），前端保持同源即可，不需要跨域配置。

# 与页面同源，由 Nginx 反向代理到后端
VITE_API_BASE_URL=/api

# 留空 = 与页面同源。HTTPS 站点下会自动使用 wss://
# VITE_WS_BASE_URL=
